# general scripts

.stack_command:
  script:
   - echo $HOSTNAME
   - cd $EXECUTION_PATH
   - echo $EXECUTION_PATH
   - cd $SERVICE1
   - echo "Finishing..."
   - echo $SERVICE1
   - $DOCKER_COMMAND1
   - echo "Finishing..."
  #  - echo "Building..."
  #  - echo $SERVICE3
  #  - cd ../$SERVICE3
  #  - $DOCKER_COMMAND1
  #  - $DOCKER_COMMAND2
