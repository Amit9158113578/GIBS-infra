.args:
  extends:
    ./gitlab-ci/.variables.yml

"${HOSTS}"
"${ELASTICSEARCH_HOSTS}"
"${KIBANA_XPACK_ENCRYPTIONKEY}"
"${ELASTICSEARCH_PASSWORD}"
"${ELASTICSEARCH_USERNAME}"
${KIBANA_URL}
"{{ lookup('ENV','ELK_VERSION') }}"
"{{ lookup('ENV','ELASTICSEARCH_USERNAME') }}"
"{{ lookup('ENV','ELASTICSEARCH_PASSWORD') }}"